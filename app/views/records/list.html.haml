.page-header
  %h1 Records

%table.table.table-striped
  %thead
    %tr
      %th Projekt
      %th Farbe
      %th Beginn
      %th Ende
      %th Dauer
      %th &nbsp;
  %tbody
    - @records.sort.each do |record|
      %tr
        %td= record.project.name
        %td{style: "background-color:#{record.project.color}"} &nbsp;
        %td= time_start record.begun_at
        %td= time_end record.ended_at
        %td= time_diff(record.begun_at, record.ended_at)
        %td
          = link_to edit_project_record_path(record.project, record) do
            = fa_icon 'edit'
          = fa_icon 'fw'
          = link_to project_record_path(record.project, record), method: :delete, data: { confirm: 'Wirklich löschen' } do
            = fa_icon 'times'

.alert.alert-information
  Records können auf den Projektseiten hinzugefügt werden.
